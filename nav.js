$(function() {
    $('header').load('head.html');
    $('footer').load('footer.html');
    // $('footer').append('<button onclick="topFunction()" id="topBtn">Top</button>')
});

$(document).ready(function () {

    // $('#home').css('text-decoration', 'underline');
    //for each of the navigation elements:
    // var navItems = $('nav ul li');

    // function addClickListener(items) {
    //     items.array.forEach(element => {
    //         element.click(function () {
    //             $('#bodyContent').load(element.id + '.html');
    //         })
    //     });
    // }

    // addClickListener($('nav ul li'));
});